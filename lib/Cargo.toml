[package]
name = "lib"
version = "0.1.0"
edition = "2021"

["lib"]
crate-type = ["cdylib", "lib", "staticlib"]


[dependencies]
uniffi = { version = "0.25.0" }

[build-dependencies]
uniffi = { version = "0.25.0", features = ["build"] }
